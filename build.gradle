apply plugin: "java"

repositories {
	mavenCentral()
	mavenLocal()
}

archivesBaseName = "tinfer"
group = "cpup.tinfer"
version = "dev"

dependencies.compile "org.scala-lang:scala-library-all:2.11.7"
apply plugin: "scala"

apply plugin: "application"
mainClassName = "cpup.tinfer.Main"

apply plugin: "idea"
apply plugin: "maven"
